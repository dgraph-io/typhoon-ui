/** @jsx jsx */
import { jsx } from "@emotion/react";
import { SelectPropsType } from "./Select.types";
declare const Select: ({ selected, label, values, fullWidth, onSelect, disabled, maxHeight, heightOffset, buttonStyles, listStyles, styles: stylesProp, }: SelectPropsType) => jsx.JSX.Element;
export default Select;
