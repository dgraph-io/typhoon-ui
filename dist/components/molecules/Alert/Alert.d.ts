/// <reference types="react" />
import { AlertPropsType } from "./Alert.types";
declare const Alert: ({ variant, heading, children, styles: stylesProp, }: AlertPropsType) => JSX.Element;
export default Alert;
