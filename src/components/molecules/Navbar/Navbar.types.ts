export interface NavbarPropsType {
    children?: React.ReactNode;
    styles?: Object;
}

export interface NavbarLogoPropsType {
    src: string;
    alt: string;
    styles?: Object;
    link?: string;
}
